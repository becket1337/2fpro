@page
@model robotsTxtModel
@using Microsoft.Extensions.Configuration
@inject IConfiguration Configuration
@{
    Layout = null;
    this.Response.ContentType = "text/plain";

    var config = Configuration.GetValue<string>("SiteConfig:SitePath");
}
<environment include="Development,Staging">

    Disallow: /

</environment>
<environment include="Production">
    User-agent:*
    Disallow: /Account
    Disallow: /Signalr
    Disallow: /LiveChat
    Disallow: /Consultant
    Disallow: /Error
    Disallow: /Order
    Disallow: /Admin
    Disallow: /Config
    Disallow: /Widget

    Host: @config
    Sitemap: @config/sitemap.xml
</environment>