
@{ 
    string[] list = ViewBag.fileList;

}
@if (list != null || list.Length > 0)
{
@foreach (var item in list)
{
    <img src="~/Content/Files/Pages/примеры/@System.IO.Path.GetFileName(item)" />
}
}